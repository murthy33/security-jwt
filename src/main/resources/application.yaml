spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/practice
    username: root 
    password: root
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true 

server:
  port: 9091